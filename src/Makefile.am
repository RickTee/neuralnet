bin_PROGRAMS = ann
ann_SOURCES = main.c neuron.c neuron.h ann.c ann.h
ann_CFLAGS = -Wall -export-dynamic
ann_LDFLAGS = -Wall -export-dynamic
ann_LDADD = $(MATH) -lm
ann_LIBS = 
#check_SCRIPTS = greptest.sh
#TESTS = $(check_SCRIPTS)

# Add extra files to clean
#CLEANFILES = greptest.sh
